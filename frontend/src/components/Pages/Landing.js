import React from 'react'
import "./style.css"
import Button from '@mui/material/Button';

export default function Landing() {
    return (
        <div class = "center">
            <h1>Cauldron</h1>
            <h2>No tricks, only treats.</h2>
            <Button variant="outlined">Start Tracking</Button>
        </div>
    )
}
